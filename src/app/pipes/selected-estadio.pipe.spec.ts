import { SelectedEstadioPipe } from './selected-estadio.pipe';

describe('SelectedEstadioPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedEstadioPipe();
    expect(pipe).toBeTruthy();
  });
});
